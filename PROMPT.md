# Ralph Prompt

Your task is to implement functionality as described in specs/* and fix_plan.md.

Follow fix_plan.md and choose the most important thing.

Before making changes, search the codebase (don't assume not implemented) DO NOT using subagents.

After implementing functionality or resolving problems, run the tests for that unit of code that was improved.

When the tests pass, update fix_plan.md, then git add -A and git commit with a descriptive message and git push.

Important: When authoring tests, capture WHY the test and its backing implementation is important.

DO NOT IMPLEMENT PLACEHOLDER OR SIMPLE IMPLEMENTATIONS. FULL IMPLEMENTATIONS ONLY.
