{
  "name": "oh-my-kiro",
  "version": "0.8.15",
  "description": "Multi-agent orchestration layer for Kiro CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "omk": "bin/omk.js"
  },
  "scripts": {
    "build": "tsc",
    "check:no-unused": "tsc -p tsconfig.no-unused.json",
    "dev": "tsc --watch",
    "lint": "biome lint .",
    "setup": "node bin/omk.js setup",
    "doctor": "node bin/omk.js doctor",
    "ask:claude": "./scripts/ask-claude.sh",
    "ask:gemini": "./scripts/ask-gemini.sh",
    "test:node": "node --test $(find dist -name '*.test.js')",
    "test": "npm run build && npm run test:node && node scripts/generate-catalog-docs.js --check",
    "coverage:team-critical": "npm run build && c8 --all --src dist/team --src dist/state --include 'dist/team/**/*.js' --include 'dist/state/**/*.js' --exclude '**/__tests__/**' --reporter=text-summary --reporter=lcov --reporter=json-summary --report-dir coverage/team --check-coverage --lines=78 --functions=90 --branches=70 --statements=78 node --test $(find dist/team/__tests__ dist/state/__tests__ -name '*.test.js')"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin/",
    "dist/",
    "agents/",
    "skills/",
    "prompts/",
    "templates/",
    "scripts/"
  ],
  "keywords": [
    "kiro",
    "kiro-cli",
    "cli",
    "agents",
    "orchestration",
    "multi-agent"
  ],
  "author": "Peng Huo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/penghuo/oh-my-kiro.git"
  },
  "homepage": "https://penghuo.github.io/oh-my-kiro",
  "bugs": {
    "url": "https://github.com/penghuo/oh-my-kiro/issues"
  },
  "license": "MIT",
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/node": "^22.19.11",
    "c8": "^10.1.3",
    "typescript": "^5.7.0"
  },
  "overrides": {
    "ajv": ">=8.18.0",
    "hono": ">=4.11.10"
  }
}
